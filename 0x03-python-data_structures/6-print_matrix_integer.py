#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    if not matrix:
        return None
    for submatrix in matrix:
        if len(submatrix)  == 0:
            Print()
            for i in range (len(submatrix) ):
                Print (“{:d}”.format(submatrix[i])),end=”\n” if I is len (submatrix) 

